{
  "buildCommand": "cd logic-tech-frontend && npm install && npm run build",
  "outputDirectory": "logic-tech-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
